%h1 Listing follows

%table#followTable.display{:style => "width:100%"}
  %thead
    %tr
      %th Follower id
      %th Followee id
      %th Show
      %th Edit
      %th Delete

  %tbody
    - @follows.each do |follow|
      %tr
        %td= follow.follower_id
        %td= follow.followee_id
        %td= link_to 'Show', follow
        %td= link_to 'Edit', edit_follow_path(follow)
        %td= link_to 'Destroy', follow, method: :delete, data: { confirm: 'Are you sure?' }

%br

= link_to 'New Follow', new_follow_path

:javascript
  $(document).ready(function() {
    $('#followTable').DataTable( {
        "scrollY":        "50vh",
        "scrollCollapse": true,
        "paging":         false
    } );
  });